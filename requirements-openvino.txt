# Optional Intel acceleration for embedding/reranker export/inference.
# Install this in addition to requirements.txt if you want VERITAS to use
# local OpenVINO models on Intel GPU.
openvino>=2026.0.0
optimum-intel>=1.27.0
nncf
